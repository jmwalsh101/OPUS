import { createContext } from "react";

export const backendDocumentsContext = createContext([]);

export const usedComponentsContext = createContext([]);

export const documentTitleContext = createContext("");

export const documentAuthorContext = createContext("");

export const documentCreatedDateContext = createContext("");

export const documentUpdaterContext = createContext("");

export const documentLastUpdatedContext = createContext("");
